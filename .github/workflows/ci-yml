name: Use Secrets

on: [workflow_dispatch]
 
jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - name: Use secret

        run: |

          echo "Username is ${{ secrets.my_pasword }}"

          

        env:

          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
 
